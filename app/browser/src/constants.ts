/**
 * Constants shared between the client and server.
 */

const CookieAuthKeyAuthenticated = "auth";
const CookieAuthValueAuthenticated = "authenticated";

export {
  CookieAuthKeyAuthenticated,
  CookieAuthValueAuthenticated
};
